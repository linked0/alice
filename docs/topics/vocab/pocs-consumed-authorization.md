| Expression | 뜻 · 쓰이는 자리 |
|---|---|
| to consume (an authorization) | 권한을 소비하다 — 썼다고 표시해 재사용을 막다 |
| replay | 재생 — 이미 쓴 서명·요청을 다시 제출하는 공격 |
| nonce | 한 번만 쓰는 번호 (number used once) |
| bitmap | 비트맵 — 한 워드의 비트마다 하나씩 상태를 담는 구조 |
| nullifier | 소각 표식 — 무엇을 썼는지 숨기고 썼다는 사실만 남기는 값 |
| domain separator | 도메인 구분자 — EIP-712에서 서명의 적용 범위를 고정하는 값 |
| chain binding | 체인 바인딩 — 서명이 특정 체인에만 유효하게 묶는 것 |
| bearer instrument | 무기명 증서 — 가진 사람이 곧 권리자 |
| to bump (a counter) | 카운터를 한 칸 올리다 · 과거 서명을 한꺼번에 무효화하는 동작 |
| outstanding | 미결의, 아직 처리되지 않은 · "every outstanding signature" |
| race (condition) | 경쟁 상태 — 순서에 따라 결과가 달라지는 버그 |
| atomic | 원자적 — 전부 되거나 전부 안 되거나 |
| durable | 영속적인 — 재시작해도 남는 |
| backstop | 최후의 보루 · "expiry is the backstop" |
| bookkeeping | 부기, 기록 관리 — 지루해서 리팩터링에 빠지는 코드 |
| to refactor out | 리팩터링하면서 빼 버리다 |
| blast radius | 피해 반경 |
| red flag | 경고 신호 |
| to flatten (a detail) | 디테일을 뭉개다 · UI가 중요한 차이를 없애 버릴 때 |
| reentrancy | 재진입 — 다른 유명한 버그 클래스, 여기선 아니었다 |
