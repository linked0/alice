| Expression | 뜻 · 쓰이는 자리 |
|---|---|
| fall short of | ~에 미치지 못하다 · "real hashes fall short of that ideal" |
| heuristic | 엄밀한 증명이 아닌 경험적인 · "such proofs only give heuristic assurance" |
| forgeable | 위조 가능한 · "is forgeable; you need a two-stage construction" |
| prepend | (맨) 앞에 붙이다 · "a prepended secret as a MAC" |
| replay across contexts | 다른 맥락에 그대로 재사용/도용하다 · "from being replayed across contexts" |
| ambiguity | 모호함, 여러 해석 가능성 · "concatenating variable-length fields creates ambiguity" |
| uniquely determined | 유일하게 결정되는 · "parsing uniquely determined" |
| MAC | 메시지 인증 코드(Message Authentication Code) · 메시지 무결성·인증을 위한 값, 단순 해시로 만들면 위조 가능. "a prepended secret as a MAC is forgeable" |
| HMAC | 해시 기반 메시지 인증 코드(Hash-based MAC) · 길이확장공격을 막는 2단계 MAC 구성. "you need a two-stage construction like HMAC" |
| SHA-256 | SHA-256(Secure Hash Algorithm 256-bit) · 머클-담가드 구조의 대표 해시 함수, 길이확장공격에 취약. "for Merkle-Damgård hashes like SHA-256" |
| Merkle-Damgård | 머클-담가드 구조(Merkle-Damgård construction) · 내부 상태가 곧 출력이라 길이확장공격에 노출되는 해시 설계 방식. "for Merkle-Damgård hashes like SHA-256" |
| Keccak/SHA-3 | 케착/SHA-3(Keccak/SHA-3) · 스펀지 구조 기반 해시, 길이확장공격에 안전한 대안. "a sponge construction like Keccak/SHA-3" |
<!-- acronyms 2026-09-18 -->
