| Expression | 뜻 · 쓰이는 자리 |
|---|---|
| knee (of a curve) | (그래프가) 꺾이는 지점, 한계점 · 성능이 급격히 나빠지기 시작하는 지점. "the knee (where p99 falls off)" |
| absorb a spike | 급증한 트래픽을 흡수하다 · 갑작스런 부하를 시스템이 버텨낼 때. "how long before new instances absorb a spike" |
| flip one knob at a time | 설정값을 하나씩 바꿔보다 · 변수를 한 번에 하나만 바꾸며 실험할 때. "Flip one knob at a time" |
| blast radius | (장애의) 영향 반경, 피해 범위 · 문제가 터졌을 때 얼마나 넓게 퍼지는지. "the ceiling and the blast radius of a runaway" |
| cold start | 콜드 스타트, 쉬다가 처음 요청받을 때 생기는 지연 · 스케일이 0에서 올라올 때의 첫 지연. "cold-start time" |
| replace a guess with a curve | 추측을 실측 데이터(곡선)로 대체하다 · 감이 아니라 측정값으로 판단할 때. "it replaces a guess with a curve" |
| a slogan, not a number | 근거 없는 구호일 뿐 실측치가 아닌 것 · 마케팅 문구와 실제 데이터를 대비할 때. "the architecture choice is a number, not a slogan" |
| scales to zero | 트래픽이 없을 때 인스턴스가 0까지 줄어드는 것 · 클라우드가 흔히 내세우는 자동 축소 기능. "'Scales to zero'... are marketing" |
| GCP | 구글 클라우드 플랫폼(Google Cloud Platform) · Cloud Run·GKE가 속한 구글의 클라우드 인프라, Jayverse가 현재 쓰는 곳. "GCP Cloud Run / GKE, or AWS" |
| AWS | 아마존 웹서비스(Amazon Web Services) · Fargate·EKS가 속한 아마존의 클라우드 인프라, GCP의 대응군. "the AWS twins are Fargate / App Runner" |
| GKE | 구글 쿠버네티스 엔진(Google Kubernetes Engine) · HPA로 오토스케일링하는 구글의 매니지드 쿠버네티스. "GKE HPA; the AWS twins are Fargate" |
| EKS | 아마존 쿠버네티스 서비스(Elastic Kubernetes Service) · GKE의 AWS 대응, HPA 기반 오토스케일링 대상. "AWS Fargate / EKS), drive synthetic load" |
| HPA | 수평 파드 오토스케일러(Horizontal Pod Autoscaler) · 쿠버네티스에서 부하에 따라 파드 수를 자동 조절하는 컨트롤러. "GKE HPA; the AWS twins" |
| RPS | 초당 요청 수(Requests Per Second) · 부하 시험에서 트래픽 강도를 나타내는 기본 단위. "Drive load low → high RPS with k6" |
<!-- acronyms 2026-09-18 -->
