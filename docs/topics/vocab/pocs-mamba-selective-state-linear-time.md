| Expression | 뜻 · 쓰이는 자리 |
|---|---|
| SSM | 상태공간 모델(State Space Model) · 시퀀스를 고정 크기 은닉 상태의 갱신으로 처리하는 모델 계열. "replaces it with a selective state-space model" |
| selective | 선택적인(입력에 따라 무엇을 남길지 고르는) · Mamba의 핵심 기여를 한 단어로. "The word selective is the contribution" |
| quadratic | 이차의(N²로 커지는) · 비용이 길이의 제곱으로 느는 것을 말할 때. "instead of quadratically" |
| KV cache | 키-값 캐시(key-value cache) · 트랜스포머가 토큰마다 보관하는 어텐션용 메모리. "the key-value cache it must hold per token" |
| throughput | 처리량 · 단위 시간에 처리하는 토큰 수, 추론 성능의 척도. "five times the inference throughput" |
| pays for it every step | 매 단계 그 값을 치른다 · 비용이 반복해서 발생함을 말하는 관용 표현. "keeps the whole context and pays for it every step" |
| buy back | 되사오다(포기한 것을 비용을 내고 되찾다) · 하이브리드 설계의 논리. "buy back exact recall inside a cheap linear stack" |
| rolling aggregate | 롤링 집계(흐르면서 갱신되는 누적값) · 로그 재생 없이 유지하는 요약. "keeps a rolling aggregate instead of replaying the log" |
| recall | 회상(먼 과거의 정확한 값을 다시 꺼내는 능력) · 상태공간 모델의 약점을 부르는 말. "The cost is recall" |
| hybrid stack | 하이브리드 스택 · 상태공간 층과 어텐션 층을 섞은 모델 구조. "hybrid stacks with some attention layers" |
| genomics | 유전체학 · DNA 서열도 긴 시퀀스라 벤치마크로 쓰임. "strong results on audio and genomics" |
| fall behind | 뒤처지다 · 비교에서 성능이 밀릴 때. "are where pure state-space models fall behind" |
