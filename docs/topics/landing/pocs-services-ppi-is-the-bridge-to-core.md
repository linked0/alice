## en
- **Number: add the print-reading routine as a standing macro checklist.** On any PPI release, log services PPI (not the headline) and the two-line measurement — near-term cut probability and mid-next-year implied rate — as separate tracked fields.
- **Number: flag concurrent supply events on the same field.** A Treasury buyback or similar supply action the same week as a print mixes into the rate reaction, so record it alongside the print rather than letting it silently confound the read.
- **Verex: don't let a single macro headline move margin or risk parameters.** If Verex's risk settings ever react to rate expectations, gate that on the path-level signal (the mid-next-year line), not the meeting-level noise this PoC distinguishes.

## ko
- **Number: 지표 읽는 루틴을 상시 매크로 체크리스트로 추가한다.** PPI 발표가 있을 때마다 헤드라인이 아니라 서비스 PPI와, 단기 인하 확률·내년 중반 내재 금리라는 두 줄 측정치를 각각 별도 필드로 기록한다.
- **Number: 같은 필드에 동시 발생하는 공급 이벤트를 표시한다.** 발표와 같은 주에 있었던 국채 바이백 같은 공급 행위는 금리 반응에 섞여 들어가므로, 읽기를 조용히 왜곡하게 두지 말고 발표와 나란히 기록한다.
- **Verex: 매크로 헤드라인 하나로 마진이나 리스크 파라미터를 움직이지 않는다.** Verex의 리스크 설정이 금리 기대에 반응한다면, 이 PoC가 구분하는 미팅 레벨 노이즈가 아니라 경로 레벨 신호(내년 중반 라인)에 걸어야 한다.
