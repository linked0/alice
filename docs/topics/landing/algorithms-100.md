## en
- **Verex: write the threat model for oracle-result finalization now.** Name who can change a result, under what conditions a dispute is possible, and what the residual risk is — the practical-connection paragraph names this boundary directly, and it is currently undocumented.
- **Auditor: adopt the ADR-plus-threat-model pair as the row's standard deliverable.** One document per major Jayverse decision — bridge relayer trust, wallet custody mode, resolution source — gives the Auditor row something concrete to check against instead of a prose claim.

## ko
- **Verex: 오라클 결과 확정에 대한 threat model을 지금 작성한다.** 누가 결과를 바꿀 수 있는지, 어떤 조건에서 이의제기가 가능한지, 잔여 리스크가 무엇인지 명시한다 — 실무 연결 문단이 바로 이 경계를 지목하는데 지금은 문서화돼 있지 않다.
- **Auditor: ADR+threat model 쌍을 그 행의 표준 산출물로 채택한다.** 브릿지 릴레이어 신뢰 모델, 월렛 커스터디 모드, 정산 소스 같은 Jayverse의 주요 결정마다 문서 하나씩을 두면 Auditor 행이 산문 주장 대신 대조할 구체적인 것을 갖는다.
