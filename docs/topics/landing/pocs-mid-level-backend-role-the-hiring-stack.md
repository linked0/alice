## en
- **CI: treat tech-debt paydown as a gitboard-tracked item.** Jayverse's CI already covers frozen lockfiles and automated deploys, which this JD lists as core competencies; add incremental tech-debt paydown as a recurring gitboard item the same disciplined way, not an afterthought.
- **Number: reuse the have/gap/not-my-direction table for Jayverse's own stack.** Run the same two-column skills diff against Jayverse's TypeScript/Solidity stack to decide where a REST+GraphQL layer (e.g. for Number's API) would actually benefit from conventional backend practice versus where it doesn't transfer.
- **Rabbit/Verex: scope any backend hire against this stack gap.** If Jayverse ever brings on backend help, decide upfront whether the role targets the TypeScript/Solidity services or a conventional Java/Spring-shaped layer like internal admin tooling, since this JD shows those are genuinely different hires.

## ko
- **CI: 기술 부채 상환을 gitboard가 추적하는 항목으로 다룬다.** Jayverse의 CI는 이미 이 채용 공고가 핵심 역량으로 꼽는 락파일 프리즈와 자동 배포를 갖추고 있다. 점진적 기술 부채 상환도 같은 방식으로 gitboard의 반복 항목에 추가하고, 나중으로 미루지 않는다.
- **Number: have/gap/not-my-direction 표를 Jayverse 스택에 재사용한다.** 같은 2열 스킬 갭 분석을 Jayverse의 TypeScript/Solidity 스택에 적용해, REST+GraphQL 레이어(예: Number의 API)가 관습적인 백엔드 실무에서 실제로 이득을 보는 지점과 그렇지 않은 지점을 구분한다.
- **Rabbit/Verex: 백엔드 채용 범위를 이 스택 갭에 맞춰 정한다.** Jayverse가 백엔드 인력을 영입한다면, 그 역할이 TypeScript/Solidity 서비스를 겨냥하는지 아니면 내부 어드민 툴링 같은 관습적 Java/Spring형 레이어를 겨냥하는지 미리 정한다. 이 공고는 둘이 실제로 다른 채용임을 보여준다.
