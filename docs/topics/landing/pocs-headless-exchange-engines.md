## en
- **Verex: track the gap list, not just the matching engine.** Verex's CLOB and market maker are the engine; read this page's partner-list logic against it and track liquidity supply, demand/order flow, and regulatory surveillance as explicit unbuilt columns, not assumed inputs.
- **Verex: budget the generalization cost if Verex is ever rented out.** Every market-specific rule Verex can hardcode today (a margin rule, a settlement window, a dispute path) becomes configuration the day it's offered as an engine other venues run on — decide now whether that's the direction before those constants pile up.
- **Devnet: apply the same lens if devnet is ever offered as shared infrastructure.** A "run your chain here" pitch turns every venue-specific setting into a load-bearing configuration surface for devnet too — note this before positioning devnet as anything beyond a single-tenant target chain.

## ko
- **Verex: 매칭 엔진뿐 아니라 갭 리스트도 추적한다.** Verex의 CLOB과 마켓 메이커는 엔진에 해당한다. 이 글의 파트너 리스트 논리를 대입해 유동성 공급, 수요/주문 흐름, 규제 감시를 가정된 입력이 아니라 명시적으로 아직 만들지 않은 항목으로 추적한다.
- **Verex: 언젠가 엔진으로 대여된다면 일반화 비용을 미리 계산한다.** 지금은 하드코딩할 수 있는 마켓별 규칙(마진 규칙, 정산 윈도우, 분쟁 경로) 하나하나가 다른 벤처가 위에서 운영하는 엔진으로 제공되는 순간 설정값이 된다. 그 방향으로 갈지 이 상수들이 쌓이기 전에 결정한다.
- **Devnet: devnet이 공유 인프라로 제공된다면 같은 관점을 적용한다.** "여기서 체인을 운영하라"는 제안은 devnet에서도 벤처별 설정 하나하나를 부담이 큰 구성 표면으로 바꾼다. devnet을 단일 테넌트 대상 체인 이상으로 포지셔닝하기 전에 이 점을 적어둔다.
