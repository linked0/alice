## en
- **Verex: carry the same ∀-form invariant into both the Foundry test and Halmos.** Write today's exercise invariant once in ∀ form and reuse the identical statement in the Foundry invariant test and, once scoped, the Halmos property — one symbol, two places, so spec and code cannot silently drift apart.
- **Auditor: state resolution preconditions as ∀/∃, not prose.** Write "what was checked, by which rule" for market resolution as an explicit quantified proposition in the methodology doc, so it is machine-checkable rather than something a reader has to interpret.
- **Number: state a reading's validity condition as a quantified proposition first.** Before wrapping a reading in a licensed-object token with expiry, write its validity condition ("defined for every day markets are open") in ∀ form — that's the form the licence/expiry token needs to enforce mechanically.

## ko
- **Verex: 같은 ∀ 형식 인바리언트를 Foundry 테스트와 Halmos 양쪽에 그대로 가져간다.** 오늘 연습의 인바리언트를 ∀ 형식으로 한 번 쓰고, 같은 문장을 Foundry 인바리언트 테스트와 (범위가 잡히면) Halmos 속성에 그대로 재사용한다. 기호 하나, 두 곳 — 스펙과 코드가 조용히 어긋나지 않게 한다.
- **Auditor: 정산 전제조건을 산문이 아니라 ∀/∃로 적는다.** 마켓 정산에서 "무엇을 어떤 규칙으로 확인했는지"를 방법론 문서에 명시적 정량화 명제로 적어, 독자가 해석해야 하는 문장이 아니라 기계적으로 검사 가능한 문장으로 만든다.
- **Number: 읽기 자료의 유효 조건을 먼저 정량화 명제로 적는다.** 읽기 자료를 만기가 있는 라이선스 객체 토큰으로 감싸기 전에, 그 유효 조건("마켓이 열린 모든 날에 정의됨")을 ∀ 형식으로 적는다. 라이선스/만기 토큰이 기계적으로 강제해야 할 형태가 바로 이것이다.
