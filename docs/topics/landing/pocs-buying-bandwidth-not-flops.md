## en
- **Rabbit/agent tooling: rent before buying local inference hardware.** Jayverse's agent flows already call the Anthropic API for planning and execution — apply this card's cloud-vs-local ledger before considering any local box for future agent compute.
- **Number: sanity-test any future indicator model as a hosted call first.** If Number ever needs its own model beyond the Anthropic API, run it hosted for ten minutes before downloading weights, per the pipeline's first step.
- **gitboard: keep a cost ledger for AI compute.** Log Claude API spend (and any future local hardware hour) the way the PoC's ledger tracks cloud vs local, so the buy-vs-rent question stays a number, not a guess.

## ko
- **Rabbit/에이전트 툴링: 로컬 추론 하드웨어를 사기 전에 대여한다.** Jayverse의 에이전트 플로우는 이미 계획과 실행에 Anthropic API를 쓴다. 향후 로컬 박스를 고려하기 전에 이 카드의 클라우드 대 로컬 장부부터 적용한다.
- **Number: 향후 지표 모델은 먼저 호스팅 호출로 검증한다.** Number가 Anthropic API를 넘어서는 자체 모델이 필요해지면 가중치를 내려받기 전에 파이프라인의 첫 단계대로 10분간 호스팅으로 돌려본다.
- **gitboard: AI 컴퓨트 비용 장부를 관리한다.** PoC의 장부가 클라우드 대 로컬을 추적하듯, Claude API 지출(과 향후 로컬 하드웨어 시간)을 기록해 구매 대 대여 질문을 감이 아니라 숫자로 유지한다.
