## en
- **Number: state the prior explicitly for any probability estimate Number publishes.** Show how the conclusion shifts under a flat versus an opinionated prior, rather than hiding the assumption inside a single point estimate.
- **Verex: log the prior alongside the posterior when a market-maker's pricing model uses Bayesian updating.** That makes a confident prior surviving weak data visible in review, not just baked silently into the quoted price.

## ko
- **Number: Number가 공개하는 모든 확률 추정치에 prior를 명시한다.** flat prior와 편향된 prior에서 결론이 어떻게 달라지는지 보여주고, 가정을 하나의 점추정 안에 숨기지 않는다.
- **Verex: 마켓메이커 가격 모델이 베이지안 업데이트를 쓴다면 posterior와 함께 prior를 기록한다.** 그래야 약한 데이터에도 살아남는 확신에 찬 prior가 호가에 조용히 녹아드는 대신 리뷰에서 드러난다.
