# Interview bank — one sentence per converse-bin item

The **converse** bin (learning-greed item, Tech #1; jay, 2026-09-21) means "learn enough to say one sentence in an interview, and no more until it touches a project". This file is where those sentences live, so they can be rehearsed as a list instead of re-derived from the pages. `add-tech-item.py --bin converse --sentence "…"` appends a row; rows are never edited in place — a better sentence is a new row. Items binned `converse` before this file existed were seeded by hand from their leads on 2026-09-21.

| Item | Title | The sentence | Added |
|---|---|---|---|
| Tech 126 | [Mixture of Experts — a model can carry 47 billion parameters and still compute with only 13 billion of them](pocs-moe-sparse-vs-active-parameters-router.html) | A mixture-of-experts model carries far more parameters than it computes with — Mixtral holds 46.7 billion and activates about 12.9 billion per token — so the router, not the parameter count, sets the cost per token. | 2026-09-21 |
| Tech 127 | [RAG or long context isn't a choice — the production pattern is filter with RAG, then attend with long context](pocs-rag-vs-long-context-hybrid-filter-then-attend.html) | RAG and long context are not rivals in production: retrieval filters the corpus down to what is relevant, and the long window lets the model attend over all of that at once. | 2026-09-21 |
| Tech 128 | [Embeddings retrieve the right context, MCP governs what an agent may do with it](pocs-embeddings-vector-db-agent-loop-mcp-primitives.html) | Embeddings decide what an agent gets to read and MCP decides what it is allowed to do; the loop between the two is where trust holds or breaks. | 2026-09-21 |
| Tech 129 | [Nigeria's first tele-robotic surgery — the 500 km distance was never the hard part, the loop was](pocs-nigeria-telesurgery-500km-latency-budget.html) | In telesurgery the 500 kilometres are not the problem; the 150-millisecond loop budget is, and jitter matters more than the mean because a surgeon's hand cannot tolerate a stall. | 2026-09-21 |
| Invest 820 | [Tokenization is the next wrapper after the mutual fund and the ETF](pocs-goldstein-blackrock-tokenization-next-wrapper.html) | Tokenization is the third wrapper after the mutual fund and the ETF — the same asset on a cheaper settlement and transfer layer — and the wallet-count projection is the part to check, not the wrapper. | 2026-09-21 |
