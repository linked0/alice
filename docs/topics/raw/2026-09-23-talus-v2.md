# Talus v2: Building Trust into Agent Execution — 2026-09-23

Claims and citations only; the body is not copied (see README.md). jay pasted the piece.

## Provenance

- **Four Pillars**, "Talus v2: Building Trust into Agent Execution", by **Jun**, **2026-08-28**,
  Crypto / Issue. **No URL captured.** Standard disclaimer. Article states it is based on the
  **v2.0.0 mainnet** release and that the spec "remains under active development".

## Mechanism (the article's description)

- DAG workflow engine on **Sui**. A task is split into **vertices**; each vertex is a tool implemented
  as an offchain HTTP service or an onchain **Move** module. Execution ends in one of several mutually
  exclusive **output variants**, which determine the next branch. An execution path is a **Walk** and
  can split, run in parallel, or repeat.
- v2 builds on v1's tool registry, bond deposits, **Signed HTTP** and scheduled execution, and binds
  caller, permitted tools, funding source and verification method to each vertex.
- **Agent** = onchain actor with one or more **skills**; the agent registry tracks active status, linked
  graph, required tools, **interface revision**, scheduled-task count.
- **Permissions**: capability declares fixed tools by registry id, checked at registration; at execution
  a permission object binds capability id + interface revision + graph + current vertex ⇒ approval
  limited to **one execution or one scheduled task**; approval tied to a task id is **copied into the
  execution object per occurrence**.
- **Budget**: locked per vertex, paid after the result is finalized. Each tool has a cashier with a
  mandatory fixed **SUI** price (zero = public), plus optional finite credits and time passes. Snapshot
  price survives mid-run price changes. Unused budget refunded; failure settles only costs incurred.
- **Scheduling**: **Task → Schedule → Occurrence**. Reserve stores per-occurrence history to prevent
  double allocation; a Task can attach a **priority fee in $US** for Leader dispatch precedence.
- **Leader**: coordinator; subscribes to onchain events, calls tools, submits results. Must register and
  **stake $US**, slashable; **primary and backup** assigned per request.
- **Three verification paths**: basic recording; signed (registered signing key proves the registered
  tool computed the outputs from legitimate inputs); external Move contract (conditions against onchain
  state, including a **zk proof against an onchain commitment**). Verifier returns accept/reject plus a
  reason hash. **The verdict controls settlement.** Decision and settlement are Sui transactions.
- Protocol state versioned at every root object; each execution pins the immutable package config
  captured at creation, so the protocol can upgrade without breaking live agents.

## Talus/acc

- With **Sui Foundation** and **Cicada Capital**; four-week cohort, winners announced **July**.
  **50+** applicants across RWA, AI, DeFi, gaming; **7** reached Demo Day.
- 1st **SuperStake** (agentic game engine, spectators bet on outcomes); 2nd **Agama** (yield layer for
  RWA tokens, underwritten credit vaults + synthetic dollar); 3rd **Noodles Finance** (Sui DeFi app).
- **The cohort used v1.** First v2 ecosystem migrations described as underway.

## The article's own proposed metrics

- Registered agents = weak measure. Instead: how often the same **Task** produces paid calls,
  verification decisions and settlement across multiple **Occurrences**; per-vertex success and refund
  rates; share of executions through external verification contracts; number and deposit distribution of
  independent Leaders; how long recurring occurrences continue; whether operators keep replenishing
  reserves.

## This note's observation (not in the article)

- All three verification paths establish **provenance and policy**, not **judgment**. A signed result is
  still whatever the model said. The characteristic agent failure is a plausible, well-formed, in-budget,
  correctly-signed wrong answer — see Eng #399 (slop grenade).
